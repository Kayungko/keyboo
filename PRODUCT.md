# Product

<!-- impeccable:product-schema 1 -->

## Platform

web（Windows 桌面载体：Tauri 2 WebView 渲染，非浏览器站点）

## Users

打字密集的人（写作者、程序员、学生）是 primary 用户：长时间敲键盘，想让输入有反馈、有陪伴。次要受众：录课/直播主播（需要按键可视化）、桌宠爱好者。

## Product Purpose

Keyboo（键啵）是桌面按键可视化与打字伙伴：实时显示按下的键与鼠标反馈，一只小角色（键啵/道童/柯基）陪你打字、攒经验、慢慢长大。成功 = 用户打字时愿意让它常驻桌面。

## Positioning

输入信号驱动的桌面陪伴——一切功能要么采集输入信号（键帽浮层、鼠标反馈），要么让输入变得有意义（伙伴经验成长、未来挂机经济）。邻近产品（Keyviz、普通桌宠）没有"输入 → 陪伴 → 成长"的闭环。

## Operating Context

- Windows 10+ 桌面常驻，托盘入口，全局快捷键 Shift+F10 暂停
- 静默模式：录屏/输密码时屏幕清空但继续收集——"屏幕不留痕"是对用户的承诺
- 浅色/深色主题，任意壁纸下的透明覆盖层
- 全中文界面，无账号，数据本地存储（plugin-store）

## Capabilities and Constraints

已有：按键浮层（4 种键帽样式 + 14 套配色）、鼠标反馈、打字伙伴（软体物理 + 经验成长 + 皮肤）、AI 额度监控（玻璃舱 HUD）、桌面便签（键帽便签待办清单）、系统集成。

桌面便签（2026-08-28 已实现，形态定稿「键帽便签」）：
- 用途：**待办清单**（一行一事项、爪印勾选划掉），非自由备忘
- 形态：**单张聚合**，默认屏幕右上角，**可拖动且位置持久化**（MVP 决策升级自"固定右上角"）
- 行内三手势：点勾选框=完成、点文本=行内编辑（Enter/Esc）、抓序号键帽=拖动排序（FLIP 让位 + 序号实时变号；键帽三态=平放/抬起/按下）
- 分页：**每页 8 条**（列表区 320px 几何上限），页码条仅超一页时出现；新增自动跳到所在页
- 完成项去向：**收纳表**——完成即移出进行中列表，头部收纳盒 icon 切换「进行中/已完成」视图（进度徽章为纯数据展示）；收纳表按完成时间倒序（今天 HH:MM/更早 M/D），可点爪印取消完成（回到进行中末尾）
- 配色：**7 色强调色选择器**（大黄蜂/绿松石/赛博/蓝/粉/月食/银），默认大黄蜂；深色键帽底不随 app 主题切换
- 伙伴关系：**轻度伙伴元素**——空状态键啵语气文案 + 爪印勾选，无交互绑定
- 在便签内打字计入伙伴经验（全局钩子天然覆盖）
- 静默模式触发时便签与键帽浮层同生命周期隐藏（Rust 直接控制窗口显隐）；暂停监听时便签保留（记录工具与输入采集无关）
- 参考竞品：PaperTodo（WPF 原生、胶囊贴边）；差异化来自伙伴绑定层

技术约束：Tauri 2 多窗口（照抄 settings 窗口模式、常驻不销毁）、z-order 须显式插入 main 之上、动画类名禁用 companion- 前缀（会被 CompanionLayer 误判）、多窗口持久化按 key 分配唯一写者（todos=note 窗口、config=settings、enabled/position=Rust）。

## Brand Commitments

- 形象：键啵 = 熊猫汤圆，#1D1D1D 身体 + #FFFDF7 白
- 触觉身份：Q 弹软体物理 + 键帽质感（PBT 双层结构是视觉签名）
- 全中文界面、无账号、本地优先
- 桌面常驻元素必须安静克制，瞬态动画优先

## Evidence on Hand

README.md（功能清单与产品叙述）、src/app.css（动画与 HUD 体系）、src/pages/settings/keycap.tsx（14 套配色 colorSchemes）、src/components/keycaps/（四种键帽结构）、src/pages/Note.tsx + src/pages/settings/note.tsx（键帽便签实现）、.tmpfiles/note-prototypes/index-v2.html（已过 design QA 的形态原型，视觉翻译源）。

## Product Principles

1. 输入信号驱动一切——新功能必须回答"它和打字有什么关系"
2. 桌面常驻元素安静克制，不与瞬态动画抢视觉层级
3. Q 弹物理与键帽质感是产品的触觉身份，跨功能保持
4. 单人维护项目，MVP 砍削优先于功能堆叠
5. 不做无法差异化的通用工具功能（差异化锚点在伙伴层）

## Accessibility & Inclusion

浅/深双主题下均需保证对比度；透明覆盖层在任意壁纸上保持可读（玻璃舱底色即为此设计）。
